{
  "name": "vendifill-node",
  "version": "1.0.0",
  "description": "mqtt handler that updates the database directly and executes calls to the api when nesicarry",
  "main": "index.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "CBM Hessels",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.7",
    "mqtt": "^5.10.1",
    "redis": "^4.7.0",
    "yaml": "^2.5.1"
  }
}
